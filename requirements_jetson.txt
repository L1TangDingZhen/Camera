# Life Tracker - Jetson Orin Nano Dependencies
# Python 3.8+ (Python 3.8 is default on JetPack 5.x)

# NOTE: PyTorch and TorchVision are installed separately via setup_jetson.sh
# because Jetson requires special prebuilt wheels from NVIDIA
# torch>=2.0.0  # Installed via setup_jetson.sh
# torchvision>=0.15.0  # Installed via setup_jetson.sh

# YOLOv8
ultralytics>=8.0.0

# Pose Estimation
mediapipe>=0.10.0

# Optional: RTMPose (GPU acceleration for Stage 2)
# Uncomment when upgrading to RTMPose
# openmim>=0.3.0
# mmcv-full>=1.7.0
# mmpose>=1.0.0

# Computer Vision
# NOTE: opencv-python may conflict with opencv installed via apt
# If you encounter issues, uninstall opencv-python and use system opencv
opencv-python>=4.8.0
opencv-contrib-python>=4.8.0
numpy>=1.24.0,<2.0
pillow>=10.0.0

# Data Processing
pandas>=2.0.0
scipy>=1.10.0

# Machine Learning (SVM classifier)
scikit-learn>=1.3.0

# Web Framework
flask>=2.3.0
flask-cors>=4.0.0

# Time Series Prediction
# NOTE: prophet may take a long time to install on Jetson
# Consider installing prebuilt wheel if available
prophet>=1.1.0

# Configuration and Logging
pyyaml>=6.0
loguru>=0.7.0

# Performance Monitoring
psutil>=5.9.0
tqdm>=4.65.0

# Visualization (optional, can be omitted for headless deployment)
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.14.0

# Utilities
python-dotenv>=1.0.0

# Jetson-specific tools (optional)
# jetson-stats  # For jtop monitoring (install via: sudo apt install python3-jetson-stats)

# Notes:
# - sqlite3 is Python standard library, no installation needed
# - For faster installation, consider using --no-cache-dir with pip
# - Some packages may have ARM64-specific build requirements
