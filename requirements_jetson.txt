# ============================================================
# Life Tracker - Jetson Orin Nano Dependencies
# ============================================================
# This file contains dependencies for NVIDIA Jetson Orin Nano Super (8GB)
# Based on current working environment as of 2025-11-28
#
# Platform Specifications:
#   - Device: NVIDIA Jetson Orin Nano Super (8GB)
#   - JetPack: 6.2.1 (R36.4.0)
#   - Kernel: 5.15.148-tegra
#   - Python: 3.10.12
#   - PyTorch: 2.8.0 (pre-installed, DO NOT install via pip)
#   - CUDA: 12.6
#   - Architecture: aarch64 (ARM64)
#
# Installation:
#   pip install -r requirements_jetson.txt
#
# IMPORTANT: PyTorch 2.8.0 is pre-installed from Jetson AI Lab PyPI
# Do NOT attempt to install torch/torchvision via pip as it will
# conflict with the Jetson-optimized version
# ============================================================

# Deep Learning Framework
# Note: PyTorch 2.8.0 already installed, DO NOT UNCOMMENT
# torch==2.8.0  # Pre-installed for Jetson
# torchvision>=0.15.0  # Pre-installed for Jetson

# Object Detection
ultralytics>=8.3.0  # YOLOv8 (tested with 8.3.229)

# Pose Estimation
mediapipe>=0.10.0  # CPU fallback (tested with 0.10.18)

# Note: RTMPose dependencies (mmcv, mmengine, mmpose) are installed separately
# See requirements_rtmpose.txt for RTMPose installation

# Computer Vision
opencv-python>=4.8.0  # Tested with 4.11.0.86
opencv-contrib-python>=4.8.0
numpy>=1.24.0,<2.0  # Tested with 1.26.4, keep <2.0 for compatibility
pillow>=10.0.0

# Data Processing
pandas>=2.0.0  # Tested with 2.3.3
scipy>=1.10.0  # Tested with 1.15.3

# Machine Learning
scikit-learn>=1.3.0  # Tested with 1.7.2

# Web Framework
flask>=2.3.0  # Tested with 3.1.2
flask-cors>=4.0.0

# Time Series Prediction
# Note: prophet compilation takes time on Jetson, be patient
prophet>=1.1.0

# Configuration and Logging
pyyaml>=6.0  # Tested with 6.0.3
loguru>=0.7.0

# Performance Monitoring
psutil>=5.9.0
tqdm>=4.65.0

# Visualization (optional for headless deployment)
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.14.0

# Utilities
python-dotenv>=1.0.0

# ============================================================
# Installation Notes
# ============================================================
#
# 1. For faster installation:
#    pip install --no-cache-dir -r requirements_jetson.txt
#
# 2. Some packages require ARM64-specific builds and may take time
#
# 3. For RTMPose support (GPU acceleration):
#    pip install -r requirements_rtmpose.txt
#
# 4. Jetson-specific monitoring tools (optional):
#    sudo apt install python3-jetson-stats  # Provides jtop command
#
# 5. Known working versions (as of 2025-11-28):
#    - ultralytics: 8.3.229
#    - mediapipe: 0.10.18
#    - opencv-python: 4.11.0.86
#    - numpy: 1.26.4
#    - pandas: 2.3.3
#    - scipy: 1.15.3
#    - scikit-learn: 1.7.2
#    - flask: 3.1.2
#    - pyyaml: 6.0.3
#
# ============================================================
